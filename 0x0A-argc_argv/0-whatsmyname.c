#include<stdio.h>

/**
 * main - Entry point
 * @argc: this is a int
 * @argv: this is a array
 * Return: always 0 (Succes)
 */
int main(int argc, char const *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
