#include<stdio.h>

/**
 * main - prints program name followd by new line
 * @argc: this is a int
 * @argv: this is a array
 * return : 0
 */
int main(int argc, char const * argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
