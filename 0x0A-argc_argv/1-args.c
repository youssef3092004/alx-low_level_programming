#include<stdio.h>

/**
 * main - Entry point
 * @argc: that is counter
 * @argv: pointer
 * Return: always 0 (Succes)
 */
int main(int argc, char *argv[])
{
(void)argv;
printf("%d\n", argc - 1);
return (0);
}
